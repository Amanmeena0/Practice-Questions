#include <bits/stdc++.h>
using namespace std;



int main() {

    int n;
    cin>>n;
    int array[];
    for(int i = 0; i < n; i++){
        cin>>array[i];
    }

    if(n == 1 || n == 0) cout<<"NOT FOUND";
    int mid = n/2;
    int left = 1;
    int right = n;

    sort(array.begin(),array.end());
    while(left < right){


    }


    return 0;
}